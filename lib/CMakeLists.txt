# Create a library called "myLib" (in Linux, this library would be
# named libmyLib.a) which includes the source file "src1.cpp".  Any
# number of sources could be listed here.
add_library (myLib src1.cpp) 
